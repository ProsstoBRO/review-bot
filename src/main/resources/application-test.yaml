bot:
  name: testName
  token: testTokent
  password: testPassword

spring:
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false
